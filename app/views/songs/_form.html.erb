<%= form_for([@artist, @song ]), remote: true do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.hidden_field :artist_id, value: @artist.id %>

  <%= f.submit "Add Song"%>

<%end%>
